<%- include('./partials/header');  %> 

<h1>Home</h1>

<% startingContent.forEach(post => { %>
    <div class="post">
        <h3><%= post.title %></h3>
        <p><%= post.body %></p>
    </div>
<% }) %>

<%- include('./partials/footer');  %> 
